"use client";import {useEffect,useState} from "react";import {api} from "../../lib/api";type Event={id:string;eventType:string;status:string;error:string|null;createdAt:string;page?:{name:string}|null};
export default function Logs(){const [items,setItems]=useState<Event[]>([]);useEffect(()=>{api<Event[]>("/webhook-events").then(setItems)},[]);return <><div className="top"><div><h1>Nhật ký</h1><div className="label">Webhook gần nhất và trạng thái xử lý</div></div></div><section className="card"><table className="table"><thead><tr><th>Thời gian</th><th>Page</th><th>Sự kiện</th><th>Trạng thái</th><th>Lỗi</th></tr></thead><tbody>{items.map(e=><tr key={e.id}><td>{new Date(e.createdAt).toLocaleString("vi-VN")}</td><td>{e.page?.name??"—"}</td><td>{e.eventType}</td><td><span className={`badge ${e.status==="FAILED"?"off":""}`}>{e.status}</span></td><td>{e.error??"—"}</td></tr>)}</tbody></table>{!items.length&&<div className="empty">Chưa có webhook</div>}</section></>}
