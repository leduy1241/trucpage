"use client";import {FormEvent,useEffect,useState} from "react";import {api} from "../../lib/api";
type Page={id:string;platformPageId:string;name:string;active:boolean;aiEnabled:boolean;provider:string;model:string};
export default function Pages(){const [items,setItems]=useState<Page[]>([]);const [error,setError]=useState("");const load=()=>api<Page[]>("/pages").then(setItems).catch(e=>setError(e.message));useEffect(()=>{void load()},[]);async function add(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await api("/pages",{method:"POST",body:JSON.stringify({name:f.get("name"),platformPageId:f.get("pageId"),provider:f.get("provider"),model:f.get("model")})});e.currentTarget.reset();void load()}return <><div className="top"><div><h1>Pages</h1><div className="label">Cấu hình Page và AI theo từng thương hiệu</div></div></div>{error&&<div className="error">{error}</div>}<div className="split"><section className="card"><h2>Thêm Page</h2><form className="form" onSubmit={add}><label>Tên Page<input name="name" required/></label><label>Platform Page ID<input name="pageId" required/></label><label>Provider<select name="provider"><option>OPENAI</option><option>DEEPSEEK</option><option>ANTHROPIC</option></select></label><label>Model<input name="model" defaultValue="gpt-4o-mini" required/></label><button>Thêm Page</button></form></section><section className="card"><h2>Danh sách Page</h2><table className="table"><thead><tr><th>Page</th><th>AI</th><th>Model</th></tr></thead><tbody>{items.map(p=><tr key={p.id}><td><strong>{p.name}</strong><div className="muted">{p.platformPageId}</div></td><td><span className={`badge ${p.aiEnabled?"":"off"}`}>{p.aiEnabled?"Đang bật":"Đã tắt"}</span></td><td>{p.provider}<div className="muted">{p.model}</div></td></tr>)}</tbody></table>{!items.length&&<div className="empty">Chưa có Page</div>}</section></div></>}
